import { Sprite } from ".";
import { RenderObjects } from "..";

function Map({ tileMap }) {
  //   return tileMap.map((tiles, i) => {
  //     // console.log(tiles.sprite);
  //     // return tiles.spriteSheet ? (
  //     return (
  //       <RenderObjects x={tiles.x} y={tiles.y} key={i}>
  //         <div className="sprite-hover-sheet">
  //           <Sprite number={tiles.sprite} spriteSheet={tiles.spriteSheet} />
  //         </div>
  //       </RenderObjects>
  //     );
  //     // ) : null;
  //   });
}
Map.displayName = "Map";
export default Map;
